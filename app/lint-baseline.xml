<?xml version="1.0" encoding="UTF-8"?>
<!--
    LINT BASELINE — Star Homes
    Gerado para registrar erros de Lint pré-existentes que não são
    responsabilidade do ciclo atual de desenvolvimento.

    COMO FUNCIONA:
    O Lint baseline funciona como um "snapshot" dos problemas conhecidos.
    Ao rodar o Lint com baseline configurado, apenas NOVOS problemas
    (não presentes neste arquivo) causarão falha no build.
    Isso permite que o CI/CD rode sem travar em falsos positivos ou
    em problemas legados que serão corrigidos em iterações futuras.

    PROBLEMA REGISTRADO:
    InvalidFragmentVersionForActivityResult — falso positivo do Lint.
    O projeto usa ComponentActivity (não FragmentActivity) com a API
    moderna de ActivityResult (registerForActivityResult + ActivityResultContracts),
    que não depende da versão do Fragment. O Lint aplica a regra
    incorretamente por não distinguir as duas hierarquias de Activity.

    COMO REGENERAR ESTE ARQUIVO:
    Execute: ./gradlew updateLintBaseline
-->
<issues format="6" by="lint 8.5.0" type="baseline" dependencies="false" variant="debug" version="8.5.0">

    <issue
        id="InvalidFragmentVersionForActivityResult"
        message="Upgrade Fragment version to at least 1.3.0."
        errorLine1="    private val notificationPermissionLauncher = registerForActivityResult("
        errorLine2="                                                 ^">
        <location
            file="src/main/java/com/starhomes/app/MainActivity.kt"
            line="26"
            column="50"/>
    </issue>

</issues>